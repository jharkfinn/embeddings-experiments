# Legacy SciFact Bridge Harness

This folder contains the older bridge-style experiment harness that was previously at the repository root.

It is kept for reference and reproducibility, but it is not the default implementation anymore.

Scope of this legacy harness:
- Qwen3.5 MoE bridge extraction
- SciFact-focused evaluation
- legacy routing and expert-pool analyses

Current/default work lives in:
- [embeddings experiment](/home/josh/Documents/embeddingsproject/embeddings%20experiment)
