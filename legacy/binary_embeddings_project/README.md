# Legacy Binary Embeddings Project

This folder contains the older binary-embeddings experiment code that used to live at the repository root.

It is retained for reference, but it is not the default implementation.

The active/default experiment now lives in:
- [embeddings experiment](/home/josh/Documents/embeddingsproject/embeddings%20experiment)
